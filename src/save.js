/**
 * Save component
 * Retorna null porque el bloque usa render dinámico (PHP)
 * El contenido se renderiza en el servidor para máxima seguridad
 */
export default function save() {
    return null;
}
